allow hal_camera_default {
  persist_camera_file
  oppo_reserve_media_camera
  oppo_reserve_media_file
}:dir { search write add_name };
allow hal_camera_default {
  persist_camera_file
  oppo_reserve_media_camera
  oppo_reserve_media_file
}:file create_file_perms;

binder_call(hal_camera_default, hal_camera_default)
binder_call(hal_camera_default, oppoalgo_daemon)

r_dir_file(hal_camera_default, vendor_proc_oppo_version)
